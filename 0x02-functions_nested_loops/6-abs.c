#include "main.h"

/**
 * _abs - gives absolute value of an integer.
 * @n: the integer to be computed.
 *
 * Return: the absolute valu of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
